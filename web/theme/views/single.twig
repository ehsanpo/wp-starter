{% extends 'layouts/base.twig' %}

{% block content %}
			<article class="layout--single">
				<div class="main-content">
					<header>
							{% if post.thumbnail %}
								<div class="news-media">
									<img src="{{post.thumbnail|resize(940, 560)}}" alt="">
								</div>
							{% endif %}
							<div class="wrapper">
								<h1>{{ post.title }}</h1>
							</div>
						
					</header>
					<section class="wrapper">
						{{ post.content }}
					</section>	
				</div>
			</article>
{% endblock %}